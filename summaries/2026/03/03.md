# Activity Summary for 3/3/2026

## 2:57:40 AM
The file `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` was logged four times between 3/2/2026, 3:29:18 PM and 3/2/2026, 10:21:53 PM. The content of the file remained consistent across all these timestamps, indicating no changes were committed during this period.

The file itself serves as a detailed grooming document or personal notes related to development tasks for a "Freshservice" project. Key information includes:

**Notification Burst Feature:**
*   **Logic Description**: Detailed pseudocode outlines a mechanism to detect a "Notification Burst" using a deque (`back[]front`) to store notification timestamps. It defines `T` (time threshold, e.g., 15 seconds) and `C` (count threshold, e.g., 3 notifications).
*   **Behavior**: If `C` notifications are received within `T` time, a `isSpecialNotificationShown` flag is used to show a special notification once or discard subsequent notifications within the burst period. If the time threshold is exceeded, the deque is cleared, and normal notifications are shown.
*   **Examples**: Several timestamp sequences (e.g., `T0=100, T5=105, T13=113, T14=114, T28=128`) are provided to illustrate burst detection scenarios and confirm adherence to acceptance criteria.
*   **Action Items (Todo / suggestions)**:
    *   Allow showing special notifications more than once.
    *   Manage notification ID and control position (top/bottom) of special notifications.
    *   Test for double account IDs and automate notifications.
    *   Refactor implementation to replace `prevNotificationTime` with a list (deque) and refine the deque/linkedlist resetting logic.
    *   Investigate injecting Gson into `NotificationBurstConverter`.
    *   Enhance special notification display (title with account name, icon, click event).
    *   Add setters/getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
    *   Suggest renaming `NotificationBurstItem` to `SafeNotificationItem`.

**App Review and Rating Prompt:**
*   **Logic**: Details how an app review prompt counter should behave:
    *   Reset and stop incrementing for 90 days after the prompt is shown.
    *   Start incrementing again after the 90-day period.
    *   Keep incrementing if the prompt hasn't been shown yet.
*   **Action Items (App Review TODO)**:
    *   Test the current app review code functionality.
    *   Refactor `AppReviewManager` and `AppStore`.
*   **Questions & Answers**: A list of answered and unanswered questions related to the app review feature and other general development points:
    *   Unanswered: Whether a separate module is needed for app review and if the `update tasks api` being called twice is a bug to fix.
    *   Answered: Confirmed asset addition is valid for changes/closing tickets, agents can create tickets, "different" detection is not needed, data loss on uninstall is acceptable, a separate PR is advised for refactoring, user on-call status doesn't need to be checked (increment on `ShiftEventActivity` open), shared preferences are to be used for persona-independent, non-account-wise counts, and the prompt should be shown on any screen where conditions are met by moving logic to `AppReviewManager`.
*   **Additional Todo (App Review related)**:
    *   Show the prompt immediately on the page where the logic condition is met.
    *   Do not increment the counter for solution articles accessed via the browser.
    *   For "seen" events, show the prompt when the user... (incomplete thought).

**Other Notes**:
*   **Branch-specific notes**: Mention of an unpushed commit to replace PNGs with SVGs and a general note to replace/update existing special notifications.
*   **MDM Browser Config**: A single item to "handle rotation."

**Patterns/Recurring Elements**:
*   A strong focus on user experience features, specifically around **notifications** (burst detection, special notification display) and **app feedback mechanisms** (review prompts).
*   Numerous **TODOs and suggestions** indicate ongoing development, refactoring, and testing requirements.
*   Use of **pseudocode and examples** to clarify complex logic.
*   **Question and Answer sections** imply active discussion and decision-making during the grooming process.

## 2:28:41 PM
The log details changes to a single file: `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md`.

The content of this file remained identical across all logged timestamps: 3/2/2026, 3:29:18 PM; 3/2/2026, 3:29:25 PM; 3/2/2026, 10:21:44 PM; and 3/2/2026, 10:21:53 PM. This indicates multiple saves or log entries of the same content without any modifications.

The file itself is a detailed markdown document outlining development notes and considerations for two primary features, along with general development tasks:

**1. Notification Burst:**
This section describes a mechanism for handling notification bursts using a deque (doubly-ended queue).
*   **Logic:** It defines parameters `T` (time threshold) and `C` (count threshold). Notifications are pushed to the front of the deque, and if `C` notifications arrive within `T` time, a "special notification" is displayed.
*   **Behavior:** Once a special notification is shown, subsequent notifications during the burst are either discarded or result in no action, ensuring the special notification is only shown once until the burst condition is reset. If the time threshold `T` is exceeded, the deque is cleared, and normal notifications resume.
*   **Examples:** Concrete examples with epoch timestamps (e.g., T=15, C=3) illustrate how different notification timings are evaluated against the burst criteria.
*   **Todo/Suggestions:** A comprehensive list of future tasks and questions includes:
    *   Allowing special notifications to show more than once.
    *   Defining notification IDs and controlling their position.
    *   Testing for double account IDs and automating notifications.
    *   Refactoring `prevNotificationTime` to use a list and improving deque reset logic.
    *   Injecting Gson into `NotificationBurstConverter`.
    *   Enhancing special notification display (title with account name, icon, click event).
    *   Adding setters/getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
    *   Considering renaming `NotificationBurstItem` to `SafeNotificationItem`.

**2. App Review and Rating Prompt:**
This section details the logic for triggering an in-app review prompt.
*   **Trigger Logic:** The prompt counter should reset upon display and not increment for the next 90 days. After this period, incrementing resumes. If the prompt hasn't been shown, the counter continues to increment.
*   **TODOs:**
    *   Verify if the current app review code is functional.
    *   Refactor `AppReviewManager` and `AppStore`.
*   **Questions (Unanswered & Answered):** A list of design and implementation questions:
    *   Whether a separate module is needed or if keeping it in the core module is acceptable (unanswered).
    *   Whether to fix the `update tasks api` being called twice (unanswered).
    *   Confirmation on adding assets to tickets and changes/closing tickets (answered: yes).
    *   How to detect "different" scenarios (answered: don't).
    *   Impact of uninstall on data (answered: no problem).
    *   Guidance on creating separate PRs for refactoring (answered: yes).
    *   How to recognize if a user is "oncall" (answered: no need, just increment on `ShiftEventActivity` open).
    *   Decision to keep it persona-independent and use shared preferences for counts (answered: yes, no account-wise or unique counts needed).
    *   Requirement to show the prompt on any screen if conditions are met, suggesting moving code from `HomeActivity` to `AppReviewManager`.
*   **Additional Todos:** Immediately show on the page where the logic was achieved, avoid incrementing for browser-based solution articles, and show the prompt for "seen" events when the user performs certain actions.

**Other Notable Entries:**
*   Mentions of "MDM Browser Config - handle rotation".
*   A pending branch change to "Replace png with svg".
*   A note to "Replace/Update existing special notification".

**Patterns and Recurring Elements:**
The document consistently uses bullet points and `*-` or `TODO:` prefixes to list tasks, suggestions, and questions. It frequently employs pseudocode and specific examples (e.g., timestamp values) to illustrate complex logic. There's a clear pattern of breaking down features into detailed requirements, implementation considerations, and follow-up actions, often structured with sections for answered and unanswered questions.