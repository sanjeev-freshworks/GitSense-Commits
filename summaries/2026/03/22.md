# Activity Summary for 3/22/2026

## 1:17:36 PM
The provided log entries for `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` are identical, both recorded on 3/20/2026 at 6:37:19 PM and 6:37:24 PM, indicating no functional change between these specific timestamps. The file serves as a detailed grooming document outlining various feature discussions, logic, and pending tasks.

Key information from the file includes:

*   **Notification Burst Logic:** A primary focus is on implementing a "Notification Burst" mechanism using a deque (double-ended queue) to detect when `C` notifications are received within `T` time.
    *   The logic involves pushing new notifications to the front of the deque and popping old ones from the back.
    *   A `isSpecialNotificationShown` flag ensures a special notification is shown only once per burst, or discarded if already shown.
    *   Examples are provided with `T = 15` and `C = 3`, illustrating time thresholds and notification sequences.
    *   The document clarifies that a special notification should be displayed upon receiving `C` notifications within `T` time.

*   **Notification-Related Todos/Suggestions:**
    *   Considerations for showing special notifications more than once.
    *   Management of notification IDs and positioning (e.g., using `setSortKey`).
    *   Testing for scenarios like double account IDs and automating notification testing.
    *   Refinements to the deque logic, including resetting and replacing `prevNotificationTime` with a list.
    *   Integration concerns like injecting `gson` in `NotificationBurstConverter`.
    *   Display enhancements for special notifications (title with account name, icon, click events).
    *   Adding setters and getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
    *   Potential renaming of `NotificationBurstItem` to `SafeNotificationItem`.

*   **Branch/Commit Information:** Mentions a pending commit to replace PNGs with SVGs and to update existing special notifications.

*   **MDM Browser Config:** A task item to handle rotation.

*   **App Review and Rating Prompt:**
    *   Detailed rules for triggering and managing the in-app review prompt:
        *   The counter should reset and not increment for 90 days after the prompt is shown.
        *   Incrementation resumes after the 90-day period.
        *   Incrementation continues if the prompt has not been shown.
    *   **App Review Todos:** Test current implementation, refactor `AppReviewManager` and `AppStore`.

*   **General Questions and Answers:**
    *   **Unanswered:** Whether a separate module is needed for a feature, and if `update tasks api` being called twice requires fixing.
    *   **Answered:** Confirms asset additions are valid for changes/closing tickets, agents can create tickets, to not detect "different" entities, data loss on uninstall is acceptable, refactoring should be a separate PR, and on-call status doesn't need to be recognized (increment when `ShiftEventActivity` opens). It also specifies using shared preferences for persona-independent counts and moving prompt display logic from `HomeActivity` to `AppReviewManager`.
    *   **Additional App Review Todos:** Show the prompt immediately upon satisfying conditions, avoid incrementing for browser cases in solution articles, and show the prompt for "seen" events (details truncated).

**Patterns and Recurring Elements:**
The document consistently uses "*-" for todo items and suggestions, indicating a checklist or brainstorming format. There's a clear emphasis on mobile application features, involving specific UI/UX elements like notifications and in-app prompts. Recurring themes include managing thresholds (time/count), handling different notification states, and ensuring robust user experience, along with considerations for refactoring and code organization within an Android development context (e.g., `ViewModel`, `Activity`, `Room DB` mentions). The detailed examples for burst detection suggest a thorough design and testing phase.