# Activity Summary for 3/20/2026

## 12:43:51 AM
The file `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` was consistently saved without any content changes between 3/18/2026, 3:24:52 PM and 3/18/2026, 4:33:46 PM. This file serves as a detailed grooming document outlining two primary features and associated development notes.

**Key Information from the File:**

1.  **Notification Burst Logic:**
    *   This section describes a mechanism for detecting a "burst" of notifications using a deque (double-ended queue) to store recent notification timestamps.
    *   Parameters `T` (time threshold) and `C` (count threshold) define a burst.
    *   A boolean flag `isSpecialNotificationShown` controls whether a "special notification" is displayed.
    *   Logic flow:
        *   Notifications are pushed to the front of the deque.
        *   If the deque length is less than `C`, it's not a burst, and normal notifications are shown.
        *   If the deque length reaches `C` and the time difference between the current notification and the oldest in the deque (`T_current - T_oldest`) is less than `T`, it's considered a burst. A special notification is shown only once per burst, and subsequent notifications within the same burst are either discarded or handled differently. The deque is then updated by popping the oldest and pushing the newest.
        *   If the time threshold `T` is exceeded, the burst condition is reset: the deque is cleared, the current notification is added, and `isSpecialNotificationShown` is set to `false`.
    *   Examples are provided with specific `T=15` and `C=3` values and illustrative timestamps to demonstrate the burst detection and notification handling.
    *   **Related Todos/Suggestions:** The team needs to address showing special notifications more than once, managing their ID and position, testing with double account IDs, automating notifications, replacing a single `prevNotificationTime` with a list, refining deque reset logic, injecting `gson`, displaying account names in special notification titles, showing icons, adding click events, and adding getters/setters in `ListNotificationViewModel`. Renaming `NotificationBurstItem` to `SafeNotificationItem` is also suggested.

2.  **App Review and Rating Prompt:**
    *   This outlines the logic for displaying an app review prompt.
    *   The prompt counter should reset when the prompt is shown and then not increment for the next 90 days.
    *   After the 90-day period, incrementing resumes.
    *   If the prompt has not been shown, the counter continues to increment.
    *   **Related Todos/Questions:** The team needs to test the current implementation, refactor `AppReviewManager` and `AppStore`, confirm if a separate module is needed (answered as "core module is fine"), address the `update tasks api` being called twice, and ensure the prompt can be shown on any screen where conditions are met by moving the logic to `AppReviewManager`. Specific conditions for not incrementing (e.g., solution articles in browser) and showing prompts for "seen" events are noted.

**Other Notes:**
*   A "branch" section mentions replacing PNGs with SVGs (commit not yet pushed) and updating existing special notifications.
*   "MDM Browser Config" mentions handling rotation.
*   Several questions related to ticket management (assets, agent creation, detecting "different" types), data persistence on uninstall, refactoring PRs, user on-call status, and preference storage were discussed and answered.

**Patterns and Recurring Elements:**
*   The document repeatedly details a feature's core logic, provides illustrative examples, and lists actionable "Todo / suggestions" or "Questions" for development.
*   The timestamps indicate a series of saves of the exact same content, suggesting that these are either routine saves or the content was stable during this period, despite being a "Post Grooming" document which might typically evolve.