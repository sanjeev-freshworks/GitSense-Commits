# Activity Summary for 7/5/2026

## 2:28:10 AM
The file `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` was repeatedly logged with identical content across multiple timestamps between July 2, 2026, 3:00:26 PM and July 3, 2026, 7:27:49 PM. This suggests a series of saves or log entries rather than active modifications to the text itself between these specific log times.

The document outlines several key development areas:

*   **Notification Burst Logic**: A detailed specification for handling "Notification Bursts." This involves using a deque to track notification timestamps, with parameters `T` (time threshold) and `C` (count threshold). The logic dictates showing a "special notification" if `C` notifications are received within `T` time, otherwise showing normal notifications or discarding. Examples are provided to illustrate the burst detection mechanism.
    *   **To-do/Suggestions for Notification Burst**: Numerous follow-up tasks are listed, including:
        *   Allowing special notifications to be shown more than once.
        *   Defining notification IDs and controlling their display position.
        *   Testing with double account IDs and automating notifications.
        *   Refining the deque/linked list resetting logic.
        *   Investigating Gson injection in `NotificationBurstConverter`.
        *   Enhancing special notification display (title with account name, icon, click event).
        *   Adding setters/getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
        *   Suggesting a rename from `NotificationBurstItem` to `SafeNotificationItem`.

*   **General Branch/Commit Notes**: Includes notes about replacing PNGs with SVGs and updating existing special notifications.

*   **MDM Browser Config**: Mentions handling rotation.

*   **App Review and Rating Prompt**: Details the logic for when an app review prompt should be displayed and when its counter should reset. The counter resets when the prompt is shown and then does not increment for 90 days. It increments if the prompt hasn't been shown or after the 90-day period.
    *   **App Review To-dos**: Tasks include testing the existing app review code and refactoring `AppReviewManager` and `AppStore`.
    *   **Questions (Answered & Unanswered)**: A section addresses various implementation and architectural questions:
        *   Unanswered questions concern the need for a separate module for app review and whether to fix the `update tasks api` being called twice.
        *   Answered questions clarify that adding assets to tickets is valid for changes/closing, agents can create tickets, "different" detection is not needed, data loss on uninstall is acceptable, refactoring should be a separate PR, and user on-call status doesn't need to be recognized (just increment on `ShiftEventActivity` open). It also states to keep the logic persona-independent and use shared preferences for counts.
    *   **Additional App Review To-dos (lowercase)**: These include immediately showing the prompt when the logic conditions are met, not incrementing for browser cases in solution articles, and showing the prompt for "seen" events (last entry incomplete).

The document serves as a comprehensive "Post Grooming" session summary, detailing proposed features, their core logic, implementation considerations, and outstanding questions or tasks across notification management and app review functionalities.

## 3:49:56 PM
The file `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` was consistently updated across multiple timestamps on July 2, 2026 (between 3:00:26 PM and 3:24:29 PM) and July 3, 2026 (between 7:05:32 PM and 7:27:49 PM). Despite these frequent saves, the textual content of the file appears identical across all provided log entries, suggesting minor non-textual changes or repeated saves without significant content alteration in the log itself.

The document details two primary areas of discussion or design:

**1. Notification Burst Logic:**
This section outlines a notification mechanism using a `deque` with thresholds `T` (time) and `C` (count) to detect a "burst" of notifications.
*   **Mechanism:** Notifications are added to the front of a deque. If the deque length is less than `C`, it's not a burst, and a normal notification is shown.
*   **Burst Detection:** If the deque length is `C` or more, and the difference between the current notification's timestamp and the oldest notification in the deque is less than `T`, it's considered a burst. A special notification is shown *only once* per burst (`isSpecialNotificationShown` flag), and subsequent burst notifications are discarded.
*   **Threshold Exceeded:** If the time difference exceeds `T`, it's not a burst; the deque is cleared, and `isSpecialNotificationShown` is reset.
*   **Examples:** Specific timestamps (e.g., T0, T5, T13, T14, T28 with T=15, C=3) are provided to illustrate burst calculation and adherence to acceptance criteria.
*   **Todo/Suggestions:** This includes showing special notifications more than once, managing their ID and position, testing with double account IDs, automating notifications, replacing `prevNotificationTime` with a list, refining deque reset logic, injecting `gson` in `NotificationBurstConverter`, and enhancing special notification UI (title, icon, click events), and adding setters/getters for relevant properties in `ListNotificationViewModel`. A suggestion to rename `NotificationBurstItem` to `SafeNotificationItem` is also present.

**2. App Review and Rating Prompt:**
This section describes the logic for triggering an in-app review prompt.
*   **Counter Logic:** When the prompt is shown, its associated counter is reset and prevented from incrementing for the next 90 days. After this 90-day period, the counter resumes incrementing. If the prompt has not yet been shown, the counter continuously increments.
*   **App Review TODOs:** Tasks include testing the current app review code and refactoring `AppReviewManager` and `AppStore`.
*   **Questions (Answered & Unanswered):** Discussions cover whether a separate module is needed (core module is fine), fixing duplicate API calls for update tasks (implied yes), validation for adding/changing/closing assets to tickets (yes), agent ticket creation (fine), detecting "different" (don't need to), data loss on uninstall (not an issue), creating a separate PR for refactoring (yes), recognizing on-call users (no need, increment on `ShiftEventActivity` opening), keeping it persona independent, and using shared preferences for counts (no account-wise/unique counts).
*   **Further Todo Items:** Immediately showing the prompt on the page where the logic condition is met, avoiding incrementing for browser cases in solution articles, and showing the prompt for "seen" events (last condition is incomplete).

**Patterns and Recurring Elements:**
The document consistently uses markdown formatting for headings and list items. Both major sections follow a similar structure: defining logic, providing examples, and then listing open "Todo/suggestions" and "Questions" (some with answers), indicating a living document used for ongoing feature development and decision-making during a grooming process. The recurring presence of detailed implementation notes alongside high-level feature descriptions suggests an iterative development approach where design decisions and potential issues are captured early.