# Activity Summary for 7/24/2026

## 1:18:54 PM
The provided log details changes to a single file: `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md`. The content of this file remained identical across all recorded timestamps, ranging from 7/16/2026, 5:11:13 PM to 7/23/2026, 6:13:20 PM, indicating multiple saves or reviews of the same document rather than modifications.

The document itself is a "Post Grooming" note outlining various features, their logic, open questions, and suggestions for development. Key information within this file includes:

*   **Notification Burst Logic:** This section details a mechanism to detect and handle "bursts" of notifications. It employs a deque (double-ended queue) to store recent notification timestamps (`T0, T1, T2...`) and uses two parameters: `T` (time threshold, e.g., 15 seconds) and `C` (count threshold, e.g., 3 notifications).
    *   If `C` notifications are received within `T` time, it's considered a burst, triggering a "special notification" which is shown only once (`isSpecialNotificationShown` flag). Subsequent notifications in the same burst are either discarded or handled differently.
    *   If the time threshold is exceeded, the deque is cleared, and a normal notification is shown.
    *   Examples are provided (`T0=100, T5=105, T13=113, T14=114, T28=128`) to illustrate burst detection and non-burst scenarios.
    *   A list of "Todo / suggestions" focuses on enhancing special notifications (showing multiple times, ID management, positioning, displaying title with account name, icon display, click events, account ID testing, automation, replacing `prevNotificationTime` with a list, deque reset logic, `gson` injection, and renaming `NotificationBurstItem`).

*   **MDM Browser Config:** A brief mention of handling rotation for MDM Browser Config.

*   **App Review and Rating Prompt:** This section outlines the logic for displaying an in-app review prompt.
    *   The core rule is that once the prompt is shown, a counter resets and should not increment for the next 90 days. After 90 days, incrementing resumes. If the prompt hasn't been shown, the counter keeps incrementing.
    *   "App Review TODOs" include testing the existing code and refactoring `AppReviewManager` and `AppStore`.
    *   Numerous "Questions" (both unanswered and answered) discuss implementation details such as module placement, duplicate API calls (`update tasks api`), handling ticket events (adding assets, changes, closing), detecting "different" events, data loss on uninstall, PR strategy for refactoring, recognizing "oncall" users, using shared preferences for counts (persona-independent, not account-wise), and integrating prompt display logic into `AppReviewManager` instead of `HomeActivity`.
    *   Further "todo" items for app review specify immediately showing the prompt on achieving the logic, not incrementing for browser-based solution articles, and showing the prompt for "seen" events when the user meets conditions.

**Patterns and Recurring Elements:**

*   The document consistently uses "Todo / suggestions" and "Questions" sections to track ongoing tasks and discuss architectural/implementation decisions for different features.
*   There's a clear distinction between initial setup (e.g., `deque.length < C`) and ongoing logic for both notification bursts and app review prompts.
*   The emphasis across features is on precise timing, state management (e.g., `isSpecialNotificationShown`, 90-day counter), and user experience.
*   Specific technical considerations like `deque`/`linkedlist`, `gson` injection, `ListNotificationViewModel`, `R.string.special_notification`, `AppReviewManager`, and `Shared Preferences` are mentioned.

## 2:18:55 PM
The `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` file, reviewed or updated across several timestamps on 7/16/2026, 7/21/2026, and 7/23/2026, outlines plans and specifications for two main features: "Notification Burst" and "App Review and Rating Prompt," along with a minor mention of "MDM Browser Config."

**Notification Burst Feature:**
This section details a mechanism to detect and handle a "burst" of notifications. It defines `T` as a time threshold (e.g., 15 units) and `C` as a count threshold (e.g., 3 notifications). A deque data structure (`back[]front`) is used to store recent notification timestamps. The logic specifies that if `C` notifications are received within `T` time, a "special notification" should be shown. A boolean flag `isSpecialNotificationShown` ensures this special notification is shown only once per burst, otherwise, subsequent notifications within the same burst are discarded or handled differently. If the time threshold `T` is exceeded, the system reverts to showing normal notifications and resets the deque. The document includes examples to illustrate the burst detection logic.

Key tasks and suggestions for the Notification Burst feature include:
*   Allowing the special notification to be shown more than once.
*   Defining its ID and controlling its display position (currently bottom by default, top if `setSortKey` is used).
*   Testing for scenarios with double account IDs and automating notification testing.
*   Refining the deque/linked list logic and reset mechanisms.
*   Considering `gson` injection for `NotificationBurstConverter`.
*   Enhancing the special notification with an account name title, group attachment, and an icon.
*   Adding click event handling for the special notification.
*   Implementing setters and getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
*   Renaming `NotificationBurstItem` to `SafeNotificationItem`.
*   A pending commit to replace PNG images with SVG.

**MDM Browser Config:**
A brief note on handling rotation.

**App Review and Rating Prompt:**
This section details the logic for displaying an in-app review prompt. The core mechanism involves a counter that:
*   Resets and stops incrementing for 90 days once the prompt is shown.
*   Starts incrementing again after the 90-day period.
*   Continues incrementing if the prompt has not yet been shown.

App Review TODOs include:
*   Verifying the functionality of the existing app review code.
*   Refactoring `AppReviewManager` and `AppStore` components.
*   Showing the prompt immediately on the page where its display conditions are met.
*   Excluding solution articles in browser cases from incrementing the counter.
*   Showing the prompt for "seen" events when applicable.

Several questions related to the App Review feature have been discussed:
*   **Unanswered:** Whether a separate module is needed for this feature, and if a duplicate API call for "update tasks" should be fixed.
*   **Answered:**
    *   Adding assets to tickets is valid for changes and closing (yes).
    *   Agents can create tickets, not just requestors (fine).
    *   Detection of "different" is not required.
    *   Data loss on uninstall is acceptable.
    *   Refactoring should be done in a separate PR.
    *   User's "on-call" status is not needed for incrementing the counter (just increment when `ShiftEventActivity` is opened).
    *   The feature should be persona-independent and use shared preferences for counts (no account-wise or unique counts).
    *   The app review prompt should be showable from any screen where conditions are met, moving the logic from `HomeActivity` to `AppReviewManager` with context passed as an argument.

**Patterns/Recurring Elements:**
The document consistently uses "Todo / suggestions" and "Questions" sections, clearly delineating actionable items and discussion points. The timestamps indicate a continuous development or grooming process for these features over a short period in July 2026, with the content remaining largely stable between these logged entries. The overall tone is that of an internal design or grooming document, detailing implementation considerations and design choices.