# Activity Summary for 8/12/2026

## 1:07:14 PM
The log details changes exclusively to the file `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md`. All entries, spanning from July 30, 2026, at 3:09:26 PM to August 4, 2026, at 8:33:06 PM, show identical content, suggesting either repeated saves without content modifications or continuous logging of the same document state over this period.

The content of the `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` file outlines two primary feature discussions:

1.  **Notification Burst Logic**:
    *   This section details a system for handling notification bursts using a deque (double-ended queue) with parameters `T` (time threshold) and `C` (count threshold).
    *   It describes a `isSpecialNotificationShown` flag to manage a special notification.
    *   The logic involves pushing new notifications to the front of the deque and popping from the back. A burst is detected if `C` notifications are received within `T` time.
    *   If a burst is detected, a special notification is shown (if not already), or subsequent notifications in the burst are discarded. If the time threshold is exceeded, the deque is reset, and a normal notification is shown.
    *   Examples with specific timestamps (`T0`, `T5`, `T13`, etc.) and values for `T` (15) and `C` (3) are provided to illustrate the burst detection and handling.
    *   A list of "Todo / suggestions" for this feature includes:
        *   Allowing special notifications to show more than once.
        *   Defining notification ID and controlling its position (currently defaults to bottom, can be moved to top with `setSortKey`).
        *   Testing for double account IDs and automating notifications.
        *   Replacing `prevNotificationTime` with a list (deque).
        *   Refining the deque reset logic.
        *   Checking for Gson injection in `NotificationBurstConverter`.
        *   Displaying the title with account name for special notifications and showing an icon.
        *   Adding a click event for special notifications.
        *   Adding setters/getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
        *   Renaming `NotificationBurstItem` to `SafeNotificationItem`.

2.  **App Review and Rating Prompt**:
    *   This section outlines the logic for displaying an app review prompt.
    *   The counter for the prompt should reset when shown and not increment for the next 90 days.
    *   After the 90-day period, the counter resumes incrementing.
    *   If the prompt hasn't been shown, the counter keeps incrementing.
    *   "App Review TODO" items include testing the current implementation and refactoring `AppReviewManager` and `AppStore`.
    *   A "Questions" section lists both unanswered and answered queries:
        *   **Unanswered**: Whether a separate module is needed or if keeping it in the core is fine; fixing the `update tasks api` being called twice.
        *   **Answered**: Confirming adding assets to tickets applies to changes and closing (yes); agents can create tickets (fine); how to detect "different" (don't); data loss on uninstall is acceptable (np); separate PR for refactoring (yes, as per Vasanth's review); how to recognize an on-call user (no need, increment when `ShiftEventActivity` opens); keep it persona independent; use shared preferences without account-wise or unique counts; show the prompt on any screen when conditions are met (move code from `HomeActivity` to `AppReviewManager` and provide context).
    *   Further "todo" items for app review: show immediately on the page where logic is achieved, do not increment for browser cases (solution articles), and show prompt for "seen" events.

**General Patterns**:
The document serves as a detailed grooming or planning session log, featuring specific implementation considerations, pseudo-code-like logic, examples, and outstanding questions/to-do items. It indicates active development and refinement stages for core application features related to notifications and user engagement (app reviews). The consistent content across multiple timestamps for this file suggests it's a living document updated incrementally, or saved frequently, rather than distinct, divergent changes. There are also general branch notes like "Replace png with svg commit is not pushed yet" and "MDM Browser Config - handle rotation" which appear as standalone notes at the end of the document.