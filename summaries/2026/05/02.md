# Activity Summary for 5/2/2026

## 3:15:53 PM
The log exclusively tracks changes for a single file: `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md`. All entries span from 4/27/2026, 12:48:14 PM to 4/28/2026, 12:22:07 PM. The content of the file remains identical across all these timestamps, suggesting repeated saves or snapshots of the document rather than content modifications.

The file itself is a detailed "Post Grooming" markdown document outlining two primary feature discussions:

1.  **Notification Burst Logic:**
    *   **Goal:** To display a "special notification" if `C` notifications are received within a `T` time period.
    *   **Mechanism:** Uses a deque (`back[]front`) to store notification timestamps.
        *   Notifications are added to the front of the deque.
        *   If the deque length is less than `C`, a normal notification is shown.
        *   If the deque length is `C` or more, it checks if the time difference between the newest and oldest notification (`T3 - T0`) is less than `T`.
        *   If it's a burst (`T3 - T0 < T`): A special notification is shown (only once if `isSpecialNotificationShown` is false), and the oldest notification is removed before adding the new one.
        *   If it's not a burst (time threshold `T` exceeded): A normal notification is shown, the deque is cleared, and `isSpecialNotificationShown` is reset.
    *   **Parameters:** `T = 15` and `C = 3` are used as examples.
    *   **Acceptance Criteria:** The logic is confirmed to adhere to showing a special notification for `C` notifications within `T` time.
    *   **To-do/Suggestions:** Further tasks include allowing special notifications to show multiple times, defining special notification IDs and position control, testing for double account IDs, automating notifications, refining deque/linked list reset logic, injecting Gson in `NotificationBurstConverter`, displaying account names with special notifications (and showing icons), adding click events, checking usage of the count variable, and adding setters/getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`. There is also a suggestion to rename `NotificationBurstItem` to `SafeNotificationItem`.

2.  **App Review and Rating Prompt:**
    *   **Logic:**
        *   When the prompt is shown, its counter should reset for all relevant logic and not increment for the next 90 days.
        *   After the 90-day period expires, incrementing should resume.
        *   If the prompt has not been shown, incrementing should continue.
    *   **To-do:** Test the current implementation, and refactor `AppReviewManager` and `AppStore`.
    *   **Questions (Unanswered):** Whether a separate module is needed or if keeping it in the core module is acceptable, and if an issue with the "update tasks api" being called twice should be fixed.
    *   **Questions (Answered):** Confirmed that adding assets to tickets (and changes/closing tickets) is valid, agents can create tickets, detecting "different" is not required, data loss on uninstall is acceptable, refactoring should be a separate PR, and user on-call status doesn't need to be recognized (increment when `ShiftEventActivity` is opened). The prompt should be persona-independent and use shared preferences for counts (not account-wise). The prompt should appear immediately on the page where the logic is achieved.
    *   **Additional To-do:** For solution articles, do not increment the count for browser cases. For "seen" events, show the prompt when the user... (sentence incomplete).

**Recurring Elements:**
The document consistently highlights design considerations, implementation details, and open questions or future tasks for both features. The "Notification Burst" section provides a detailed algorithm, while the "App Review" section focuses on policy and trigger conditions.