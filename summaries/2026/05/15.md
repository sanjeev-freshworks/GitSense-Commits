# Activity Summary for 5/15/2026

## 1:30:10 PM
The log details numerous updates to a single file, `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md`, occurring between April 27, 2026 (starting at 3:44:59 PM) and May 8, 2026 (ending at 10:49:33 AM).

Despite the many timestamps, the **content of the file remains identical across all recorded entries**. This indicates that while the file was repeatedly logged, no actual textual changes were introduced to the document itself within this period.

The content of the file outlines several key discussion points and tasks:

*   **Notification Burst Logic:** A detailed algorithm is described for detecting a "notification burst," defined as receiving `C` notifications within `T` time. It uses a deque (`back[]front`) to store notification timestamps.
    *   `isSpecialNotificationShown` boolean flag controls the display of a special notification.
    *   The logic covers scenarios for normal notifications, burst detection, showing/discarding special notifications, and resetting the deque.
    *   Example timelines (`T0 = epoch + 0`, `T = 15`, `C = 3`) and specific test cases are provided to illustrate the burst detection mechanism.
    *   The author states that the described logic adheres to acceptance criteria.
*   **Todo/Suggestions for Notification Burst:** A list of enhancements and questions related to special notifications:
    *   Allowing special notifications to show more than once.
    *   Determining notification ID and controlling position.
    *   Testing with double account IDs.
    *   Automating notifications.
    *   Replacing `prevNotificationTime` with a list/deque.
    *   Refining deque/linked list resetting logic.
    *   Injecting `gson` into `NotificationBurstConverter`.
    *   Displaying account name in special notification title, attaching to a group, and showing an icon.
    *   Adding click events.
    *   Verifying usage of a 'count' variable.
    *   Adding setters/getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
    *   Considering renaming `NotificationBurstItem` to `SafeNotificationItem`.
*   **Branch/Commit Status:** A note about a pending commit to "Replace png with svg" which is not yet pushed.
*   **MDM Browser Config:** A brief mention to "handle rotation."
*   **App Review and Rating Prompt Logic:** Defines rules for showing an app review prompt:
    *   Resetting a counter for every logic and preventing increment for 90 days after the prompt is shown.
    *   Resuming increment after the 90-day period.
    *   Continuing increment if the prompt hasn't been shown.
    *   **App Review TODOs:** Test existing code, refactor `AppReviewManager` and `AppStore`.
*   **Questions (Answered & Unanswered):**
    *   **Unanswered:** Whether a separate module is needed for the app review prompt, and if a double API call for update tasks should be fixed.
    *   **Answered:** Confirmed that adding assets to tickets applies to changes/closing, agents can create tickets, "different" detection is not needed, data loss on uninstall is acceptable, refactoring should be a separate PR (as per Vasanth's review), user on-call status is not required (increment when `ShiftEventActivity` is opened), the feature should be persona-independent, and shared preferences should be used for counts (not account-wise/unique).
*   **General To-Dos (App Review):**
    *   Show the prompt immediately on the page where the logic condition is met.
    *   Do not increment the counter for solution articles when accessed via a browser.
    *   Show the prompt for "seen" events.