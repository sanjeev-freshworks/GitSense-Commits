# Activity Summary for 5/10/2026

## 2:01:11 PM
The log entries consistently show the same content for the file `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md` across multiple timestamps between April 27, 2026 (starting at 3:44:59 PM) and May 8, 2026 (ending at 12:05:55 PM). This suggests that the file content remained unchanged during this period, and the log might be recording access or saving of the same document.

The `Post Grooming.md` document outlines key information related to two main features:

1.  **Notification Burst Logic:**
    *   A detailed algorithm is described for detecting "notification bursts" using a deque.
    *   It defines parameters `T` (time threshold, e.g., 15 units) and `C` (count threshold, e.g., 3 notifications).
    *   The logic specifies how to handle incoming notifications:
        *   If the deque length is less than `C`, it's not a burst; a normal notification is shown, and the new notification's timestamp is pushed to the front.
        *   If the deque length is `C` or more, it checks if the time difference between the latest and oldest notification in the deque is less than `T`.
        *   If it's a burst, a `special notification` is shown (only once if `isSpecialNotificationShown` is false). The oldest notification is removed (`pop_back`), and the new one is added (`push_front`).
        *   If it's not a burst (time threshold exceeded), a normal notification is shown, the deque is cleared, the new notification is pushed, and the `isSpecialNotificationShown` flag is reset.
    *   Examples with specific timestamps and states of the deque illustrate the burst detection process.
    *   A list of "Todo / suggestions" is included for this feature, focusing on:
        *   Displaying special notifications multiple times.
        *   Managing notification IDs and positions.
        *   Testing for double account IDs and automating notifications.
        *   Refining deque/linked list resetting logic.
        *   Considering `Gson` injection for `NotificationBurstConverter`.
        *   Customizing special notification titles, icons, and click events.
        *   Adding setters/getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
        *   Suggesting renaming `NotificationBurstItem` to `SafeNotificationItem`.

2.  **App Review and Rating Prompt:**
    *   The document details the conditions for displaying an app review prompt.
    *   The counter for the prompt should reset for 90 days after it's shown and then resume incrementing. If the prompt hasn't been shown, the counter should keep incrementing.
    *   A "TODO" section for App Review includes testing the current code and refactoring `AppReviewManager` and `AppStore`.
    *   "Unanswered Questions" highlight concerns about module placement and duplicate API calls.
    *   "Answered Questions" cover various implementation details and design decisions, such as:
        *   Validity of adding assets for ticket changes/closing.
        *   Agent's ability to create tickets.
        *   Ignoring "different" detection.
        *   Accepting data loss on uninstall.
        *   Creating separate PRs for refactoring.
        *   Incrementing the counter when `ShiftEventActivity` is opened instead of checking on-call status.
        *   Keeping logic persona-independent and using shared preferences for counts.
        *   Showing the prompt on any screen when conditions are met, moving logic from `HomeActivity` to `AppReviewManager`.
    *   Additional "todo" items specific to app review include immediate display on logic achievement, not incrementing for browser cases in solution articles, and showing the prompt for "seen" events (though the last point is incomplete).

Other minor notes mention replacing PNGs with SVGs (commit not yet pushed) and handling rotation for MDM Browser Config.

In summary, the document serves as a comprehensive "post-grooming" checklist and design specification for implementing a notification burst mechanism and an app review prompt feature within Freshservice, detailing technical logic, user experience considerations, and development tasks.

## 3:02:13 PM
The log consistently shows entries for a single file: `/Users/skumar14/workspace/Freshservice/Personal Use/Post Grooming.md`. All timestamps, ranging from **4/27/2026, 3:44:59 PM** to **5/8/2026, 12:05:55 PM**, record identical content for this file, indicating that no changes were committed or saved to this specific document during this period, or that the log captures read operations rather than modifications.

The `Post Grooming.md` file serves as a detailed documentation for several features and development tasks within a project, likely Freshservice. Key information includes:

**1. Notification Burst Logic:**
*   A mechanism to detect and handle a "notification burst" is outlined. This involves using a deque (`back[]front`) to store timestamps of incoming notifications.
*   Parameters `T` (time threshold, e.g., 15 units) and `C` (count threshold, e.g., 3 notifications) define a burst.
*   A boolean flag `isSpecialNotificationShown` manages the display of a special notification.
*   The logic specifies:
    *   If the deque length is less than `C`, it's not a burst; a normal notification is shown, and the new notification is pushed to the front of the deque.
    *   If the deque length is `C` or more, it checks if the time difference between the current notification (`T_n`) and the oldest notification (`T_0`) in the deque is less than `T`.
    *   If it's a burst, a special notification is displayed (only once if `isSpecialNotificationShown` is false), and the oldest notification is removed before adding the new one.
    *   If the time threshold `T` is exceeded, it's not a burst; a normal notification is shown, the deque is cleared, the new notification is added, and `isSpecialNotificationShown` is reset to false.
*   Examples and specific scenarios are provided to clarify the burst detection criteria, emphasizing that a special notification should be shown upon receiving `C` notifications within `T` time.

**Associated Todo/Suggestions for Notification Burst:**
*   Allow special notifications to show more than once.
*   Define a notification ID for special notifications and control their display position (currently defaults to bottom, can be moved to top with `setSortKey`).
*   Test for scenarios involving double account IDs.
*   Automate notification testing.
*   Replace a `prevNotificationTime` variable with a list (deque) for tracking.
*   Refine the logic for resetting the deque/linked list.
*   Investigate injecting Gson into `NotificationBurstConverter`.
*   Enhance special notification display (title with account name, icon visibility).
*   Add click events for special notifications.
*   Verify the usage of a "count" variable.
*   Implement setters and getters for `shouldShowSpecialNotification` and `accountId` in `ListNotificationViewModel`.
*   Consider renaming `NotificationBurstItem` to `SafeNotificationItem`.

**2. Other Development Notes:**
*   **MDM Browser Config:** A note to handle rotation.
*   **App Review and Rating Prompt:**
    *   Detailed rules for showing the prompt: reset counter upon display, then don't increment for 90 days. After 90 days, resume incrementing. If the prompt hasn't been shown, keep incrementing.
    *   **App Review TODOs:** Test current app review code, refactor `AppReviewManager` and `AppStore`.
    *   **Unanswered Questions:** Whether a separate module is needed for this feature, and if a double API call for "update tasks" should be fixed.
    *   **Answered Questions:**
        *   Adding assets to tickets is valid for changes and closing tickets.
        *   Agents can create tickets.
        *   "Different" detection is not needed.
        *   Data loss on uninstall is acceptable.
        *   Refactoring should be done in a separate PR (as per Vasanth's review).
        *   User's "on-call" status is not needed for incrementing the counter (increment when `ShiftEventActivity` is opened).
        *   The implementation should be persona-independent and use shared preferences for counts (not account-wise or unique counts).
        *   The app review prompt should be shown on any screen when its conditions are met (logic moved from `HomeActivity` to `AppReviewManager`).
    *   Additional `todo` for app review: Immediately show on the page where the logic was achieved, don't increment for solution articles in the browser, and show the prompt for "seen" events (incomplete note).